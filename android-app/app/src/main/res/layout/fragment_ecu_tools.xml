<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <TextView
            android:id="@+id/ecuToolsStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="8dp"
            android:textColor="@color/text_secondary"
            android:textSize="12sp" />

        <Button
            android:id="@+id/btnECUInfo"
            style="@style/ToolButton"
            android:text="@string/read_ecu_info" />

        <TextView
            android:id="@+id/ecuInfoText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp"
            android:text="No ECU info yet"
            android:textColor="@color/text_primary"
            android:textSize="14sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btnDealerOn"
                style="@style/ToolButton"
                android:text="@string/activate" />

            <Button
                android:id="@+id/btnDealerOff"
                style="@style/ToolButton"
                android:text="@string/deactivate" />

        </LinearLayout>

        <Button
            android:id="@+id/btnStream"
            style="@style/ToolButton"
            android:text="@string/start_stream" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btnPing"
                style="@style/ToolButton"
                android:text="@string/cmd_ping" />

            <Button
                android:id="@+id/btnStop"
                style="@style/ToolButton"
                android:text="@string/stop_communication" />

        </LinearLayout>

    </LinearLayout>
</ScrollView>
